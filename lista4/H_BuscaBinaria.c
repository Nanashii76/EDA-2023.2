#include <stdlib.h>
#include <stdio.h>


// implementnado a busca

void busca (int o[], int x[], int tam1, int tam2) {

   
   

}



int main () {

    int n,m,a = 0;
    scanf("%d %d", &n,&m);

    int ord[n];
    int bus[m];

    for (int i = 0; i < m+n; ++i) {
        if (i <= n) scanf("%d", ord[i]);
        else scanf("%d",bus[a++]);
    }



    
}